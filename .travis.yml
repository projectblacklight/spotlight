notifications:
  email: false

rvm:
  - 2.3.1
  - 2.2.5

matrix:
  include:
    - rvm: 2.3.1
      env: "RAILS_VERSION=5.0.0"

notifications:
  irc: "irc.freenode.org#blacklight"

env:
  global:
    - NOKOGIRI_USE_SYSTEM_LIBRARIES=true
    - RAILS_VERSION=4.2.6

sudo: false
language: ruby

script:
  - bundle exec rake

jdk: oraclejdk8
