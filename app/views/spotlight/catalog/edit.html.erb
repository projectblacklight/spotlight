<%= bootstrap_form_for @document, url: spotlight.exhibit_catalog_path(current_exhibit, @document) do |f| %>
  
  <%= render_document_partials @document, blacklight_config.view_config(:edit).partials, f: f %>
<% end %>

