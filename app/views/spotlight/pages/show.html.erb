<%= link_to 'Edit', spotlight.edit_page_path(@page), :class => 'btn btn-primary pull-right' %>
<h1>
  <%= @page.title %>
</h1>

<div>
  <%= render_sir_trevor(@page.content) unless @page.content.blank? %>
</div>

