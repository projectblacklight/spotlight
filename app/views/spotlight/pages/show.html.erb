<%= link_to t('helpers.submit.edit'), spotlight.polymorphic_path([:edit, @page.exhibit, @page]), :class => 'btn btn-primary pull-right' %>
<h1>
  <%= @page.title %>
</h1>

<div>
  <%= render_sir_trevor(@page.content) unless @page.content.blank? %>
</div>

